export enum MessageType {
  SERVICE = 'SERVICE',
  TEXT = 'TEXT',
  IMAGE = 'IMAGE',
  STICKER = 'STICKER',
}

export enum SenderType {
  SOURCE = 'SOURCE',
  TARGET = 'TARGET',
}

export enum MessageEventType {
  CREATED = 'CREATED',
  UPDATED = 'UPDATED',
  DELETED = 'DELETED',
}
